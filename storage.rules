rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read and write everything (most permissive)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
